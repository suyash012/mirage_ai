export const metadata = {
  title: 'AI Chat Comparison',
  description: 'Compare responses from multiple AI models in real-time',
}

export default function ChatLayout({ children }) {
  return children
}